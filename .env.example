# Environment Variables Template
# Copy this to .env and fill in your values: cp .env.example .env

# ============================================
# MongoDB Configuration
# ============================================

# OPTION 1: Local MongoDB (RECOMMENDED for assignment review)
# - No account creation needed
# - Works offline
# - Just run: npm run seed
MONGODB_URI=mongodb://localhost:27017/taskflow

# OPTION 2: MongoDB Atlas (Cloud) - Optional
# - Create free account at https://www.mongodb.com/cloud/atlas
# - Replace username, password, and cluster URL
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/taskflow?retryWrites=true&w=majority

# ============================================
# JWT Authentication
# ============================================
# Secret key for JWT tokens (change this in production!)
# To generate a secure key, run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# ============================================
# Server Configuration
# ============================================
PORT=5000
NODE_ENV=development

# ============================================
# Frontend/Backend Base URLs
# ============================================
# Local fallback is http://localhost:5000/api if VITE_API_URL is not set
VITE_API_URL=http://localhost:5000/api
FRONTEND_URL=http://localhost:5173

# ============================================
# Production example — replace with your values
# ============================================
# MONGODB_URI=<your Mongo Atlas connection string>
# JWT_SECRET=<strong random secret>
NODE_ENV=production
VITE_API_URL=https://team-taskflow.netlify.app/api
FRONTEND_URL=https://team-taskflow.netlify.app
